<?xml version="1.0" encoding="utf-8"?>
<!--
  CoreCmp library manifest — store-policy safe by design.

  This library does NOT declare sensitive permissions (camera, location, contacts, etc.).
  Host apps must declare ONLY the permissions they actually use.
  See docs/STORE_COMPLIANCE.md and docs/templates/AndroidManifest.permissions.xml
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Optional hardware — never required; avoids Play filtering on camera-less devices -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />

    <!-- Camera temp file sharing for TakePicture — authority uses host applicationId -->
    <application>
        <activity
            android:name="com.corecmp.shared.auth.GoogleSignInActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="false" />
        <provider
            android:name="com.corecmp.shared.platform.CoreCmpFileProvider"
            android:authorities="${applicationId}.corecmp.camera"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/corecmp_file_paths" />
        </provider>
    </application>

</manifest>
